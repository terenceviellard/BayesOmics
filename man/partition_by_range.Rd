% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_posterior.R
\name{partition_by_range}
\alias{partition_by_range}
\title{Partition IDs by Slicing Input into Ranges}
\usage{
partition_by_range(
  breaks = NULL,
  n_bins = NULL,
  input_col = "Input",
  input_id = NULL
)
}
\arguments{
\item{breaks}{Explicit interval breakpoints, passed to \code{cut()}. If
\code{NULL} (default), \code{n_bins} equal-width bins spanning the
observed range of \code{Input} are used instead.}

\item{n_bins}{Number of equal-width bins when \code{breaks} is \code{NULL}.
Defaults to \code{3}.}

\item{input_col}{Name of the Input column to slice. Defaults to
\code{"Input"}.}

\item{input_id}{When \code{db} has a multi-dimensional Input (an
\code{Input_ID} column with more than one distinct value), which axis to
slice on. Required in that case -- without it, guessing an axis could
silently cut on the wrong one, so \code{\link{resolve_partition}} errors
instead.}
}
\value{
A \code{partition_strategy} object usable as
\code{\link{fit_block_posterior}}'s \code{partition} argument.
}
\description{
Cuts \code{Input} values into intervals (via \code{\link[base]{cut}}); IDs
whose Input falls in the same interval go into the same block.
}
