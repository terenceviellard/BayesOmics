% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim_kernel.R
\name{fit_kernel}
\alias{fit_kernel}
\title{Optimize Hyperparameters for a kernel (with additive kernel support)}
\usage{
fit_kernel(
  kern = NULL,
  db,
  prior_mean = NULL,
  prior_cov,
  pen_diag = 1e-06,
  verbose = FALSE,
  max_iter = 1000,
  factr = 1e+07,
  pgtol = 0,
  track_trace = FALSE,
  group_col = NULL,
  pooled = TRUE,
  df_warn = 8
)
}
\arguments{
\item{kern}{\code{NULL} (default) for the closed-form fit (see \verb{@description}),
or a kernel object inheriting from keRnel's \code{kernel} class for a real
kernel fit. In the latter case, its construction values (e.g.
\code{se_kernel(length_scale = 2)}) are the optimization's starting
point -- there is no separate \code{hp} argument. Optimizing in
\code{keRnel::get_free_params(kern)}'s free/unconstrained space (rather
than by natural-space hyperparameter name) is what lets two sibling
sub-kernels sharing a bare hyperparameter name (e.g.
\code{se_kernel(length_scale=1) + se_kernel(length_scale=3)}) be given
genuinely different starting values and be fit independently -- a case
\code{keRnel::kupdate()} cannot express, since it updates every
occurrence of a bare name at once.}

\item{db}{The dataset used for optimization. Must contain columns \code{Group}
and \code{Output}; \code{ID}/\code{Input} (plus \code{Input_ID} for a multi-dimensional
Input) may be omitted together to trigger univariate mode (only
meaningful when \code{kern = NULL} -- a real kernel has nothing to fit a
spatial structure to with one dummy position per group). When \code{kern} is
a real kernel object, \code{db} must contain columns \code{Input} and \code{Output}
(plus \code{Input_ID} for a multi-dimensional Input; see
\code{\link{normalize_input_cols}}). If it also contains a \code{Sample} column
and the same \code{Input} positions are repeated across samples (i.e.
replicated measurements), the function automatically uses the correct
replicated likelihood: it builds a pxp kernel matrix on the \emph{unique}
input positions and sums independent log N(y_s; mu, K_p) terms over each
sample s. Without this correction, same-position observations in
different samples would be treated as perfectly correlated, biasing all
hyperparameter estimates.}

\item{prior_mean}{Prior mean: either a scalar or a vector of length equal to
the number of unique \code{Input} positions in \code{db}. Required unless
\code{group_col} is supplied (in which case it is ignored -- see \code{group_col}).}

\item{prior_cov}{Prior covariance: either a scalar (diagonal value) or a
square matrix of size equal to the number of unique \code{Input} positions in \code{db}.}

\item{pen_diag}{Jitter added to the diagonal for numerical stability. Defaults to \code{1e-6}.}

\item{verbose}{If \code{FALSE} (default), returns the optimized parameter vector;
if \code{TRUE}, returns the full \code{optim} result list (with the fitted kernel
object attached as \code{result$kern}).}

\item{max_iter}{Maximum number of L-BFGS-B iterations. Defaults to \code{1000}.}

\item{factr}{L-BFGS-B relative convergence tolerance, passed straight through
to \code{stats::optim()}'s \code{control$factr}. Smaller values demand tighter
convergence (more iterations); defaults to \code{1e7} (the value previously
hardcoded).}

\item{pgtol}{L-BFGS-B projected-gradient convergence tolerance, passed
straight through to \code{stats::optim()}'s \code{control$pgtol}. Defaults to \code{0}
(R's own \code{optim()} default, previously left unset).}

\item{track_trace}{If \code{TRUE}, records every objective/gradient evaluation
during the optimization (hyperparameter values, NLL or gradient norm,
elapsed time) and attaches it as a \code{trace} data.frame. Defaults to
\code{FALSE}, in which case nothing is recorded and the return value is
identical to before this parameter existed.}

\item{group_col}{Name of a column in \code{db} (e.g. \code{"Group"}) identifying
which experimental group each observation belongs to. When supplied,
pools replicates from every group into a single fit -- centering each
\verb{(group, ID)} cell on its own empirical mean first (\code{prior_mean} is then
ignored, forced to \code{0}; requires an \code{ID} column) -- and adds the exact
closed-form REML correction for the degrees of freedom spent estimating
those group means:
\eqn{\text{NLL}_\text{REML}(\theta) = \text{NLL}_\text{demeaned}(\theta) -
(G/2)\log|\Sigma'_\theta|}, \eqn{G} = number of distinct groups (NOT
groups x ids -- the correction tracks the rank of the replicate space
the mean was projected out of, unchanged whether a per-group mean is a
scalar or a full per-id vector). Without the demeaning step, pooling
multiple groups under one shared \code{prior_mean} lets an unmodeled
between-group mean shift bias the fitted kernel variance; demeaning by
the full \verb{(group, ID)} cell (rather than by group alone) is required for
that correction to hold for a realistic, non-uniform differential
pattern (only some ids shifted) -- group-alone demeaning only exactly
cancels a shift that is the SAME on every id, and otherwise leaves a
residual that inflates the fitted variance and roughly doubles the
package's own OVL metric on a realistic partial-shift pattern (confirmed
empirically, not just a theoretical concern). See
\code{dev/optim_exploration/NOTES_math.md},
\verb{dev/optim_exploration/14_group_and_id_demean/README.md} and
\code{demeaning_likelihood.tex} for the full derivation and empirical
validation (HP recovery, end-to-end OVL, joint coverage). Requires a
balanced Group x Sample design (the same features observed for every
group/sample combination); errors otherwise. Known pathological regimes
(not yet guarded against, see the README above): very small \code{N} per
\verb{(group, ID)} cell, and extreme \code{mu_random} prior spread when
\code{mu_random = TRUE} was used to simulate the data. Defaults to \code{NULL} (no
group handling, behavior identical to before this parameter existed).}

\item{pooled}{When \code{kern = NULL}: passed straight to
\code{resolve_closed_form_kernel()} -- \code{TRUE} (default) shares one estimate
across every group, \code{FALSE} gives each group its own (see
\code{resolve_closed_form_kernel()}'s own documentation; this is exactly
\code{posterior_mean()}'s own \code{pooled} argument). When \code{kern} is a real
kernel object: only used when \code{group_col} is also supplied. \code{TRUE}
(default) shares a single kernel fit across every group (see
\code{group_col}'s own documentation for how that pooled fit is built). If
\code{FALSE}, \code{db} is split by \code{group_col} and \code{fit_kernel()} is called
independently on each group's own subset (with that group's
\code{prior_mean}, \code{group_col = NULL} -- no demeaning, a genuine per-group
MLE/REML fit of the real kernel), returning a named list keyed by group
instead of a single result. Ignored (with no effect) when \code{kern} is a
real kernel object and \code{group_col} is \code{NULL}.}

\item{df_warn}{Only used when \code{kern = NULL}. Forwarded to
\code{resolve_closed_form_kernel()}/\code{check_closed_form_df()}: a \code{warning()}
is issued below this many residual degrees of freedom (the estimate
runs but is noisy), and an \code{error()} at 0 or fewer (undefined). Defaults
to \code{8} -- see \code{posterior_mean()}'s own \code{df_warn} argument.}
}
\value{
When \code{kern = NULL}: a single kernel object (\code{pooled = TRUE}) or a
named list of kernel objects keyed by group (\code{pooled = FALSE}) -- see
\code{resolve_closed_form_kernel()}; \code{verbose}/\code{max_iter}/\code{factr}/\code{pgtol}/
\code{track_trace} are not used on this path. Otherwise (a real kernel
object), if \code{verbose} is \code{FALSE}, a named vector of optimized hyperparameters
in natural (constrained) space (via \code{keRnel::get_trainable_params()}),
with the optimizer's \code{convergence} code and final objective \code{value}
attached as attributes (\code{attr(result, "convergence")}, \code{attr(result, "value")}) so convergence can be checked without re-running with \code{verbose = TRUE}; a \code{convergence} of \code{0} means success. If \code{track_trace = TRUE}, a
\code{trace} data.frame (one row per objective/gradient evaluation) is also
attached as \code{attr(result, "trace")}. Otherwise (\code{verbose = TRUE}) the full
list returned by \code{\link[stats:optim]{stats::optim()}}, with \code{result$kern} (the fitted kernel
object) and \code{result$trace} (when \code{track_trace = TRUE}) added.
}
\description{
Also the single entry point that unifies univariate and multivariate
fitting: \code{kern = NULL} delegates to the exact same closed-form fit
\code{posterior_mean(kern = NULL)} uses internally (works with or without
\code{ID}/\code{Input} -- univariate mode is triggered exactly as it is there, see
\code{inject_univariate_dummy_cols()}), while a real, unfitted kernel object
fits it via L-BFGS-B. Both branches respect \code{pooled}/\code{group_col}
identically in spirit (one shared fit vs. one independent fit per group)
and return the same shape (a single kernel object, or a named list keyed
by group), so \code{posterior_mean(data, kern = fit_kernel(kern, data, pooled = pooled, ...))} is a uniform two-step pipeline regardless of which branch
ran.
}
\examples{
data <- simu_db(nb_id = 8, nb_group = 1, nb_sample = 5)
kern <- keRnel::variance_kernel(variance = 1) * keRnel::se_kernel(length_scale = 1)
fit_kernel(kern, data, prior_mean = 0, prior_cov = 1)

# Pooling replicates from multiple groups into a single fit, without an
# unmodeled group mean shift biasing the fitted variance:
data2 <- simu_db(nb_id = 8, nb_group = 3, nb_sample = 5, diff_group = 4)
fit_kernel(kern, data2, prior_cov = 1, group_col = "Group")

# Two independent fits (one per group), each its own real, non-demeaned
# kernel HP optimization -- pooled = FALSE:
fits <- fit_kernel(kern, data2, prior_mean = 0, prior_cov = 1,
                    group_col = "Group", pooled = FALSE, verbose = TRUE)
names(fits)

# kern = NULL: the same pooled/non-pooled choice, but for the closed-form
# fit -- works identically in univariate mode (no ID/Input at all) and in
# multivariate mode (a real ID/Input design), and returns a kernel (or
# named list of kernels) ready for posterior_mean(kern = ...), exactly
# like the real-kernel branch above:
cpg <- data.frame(Group = rep(c("A", "B"), each = 5),
                   Output = c(rnorm(5, 0, 1), rnorm(5, 3, 1)))
fit_kernel(NULL, cpg, pooled = TRUE)   # one shared closed-form kernel
fit_kernel(NULL, cpg, pooled = FALSE)  # one closed-form kernel per group
}
